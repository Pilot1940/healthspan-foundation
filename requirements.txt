# HealthSpan — pinned to the tested cold-start set (v3.2).
# The CORE set works the App / supabase_client path WITHOUT psycopg2.

# --- core (App / supabase_client path) ---
supabase==2.10.0
httpx<0.28            # supabase 2.10 needs httpx<0.28 (0.28 removed the `proxies` kwarg gotrue/postgrest pass)
PyJWT==2.12.1         # pin away from the OS-managed PyJWT 2.7.0 whose RECORD conflicts on reinstall
python-dotenv==1.0.0
anthropic==0.107.0    # vision extraction in ingest/food.py, whoop_screenshot.py, brief.py

# --- direct_role / local Postgres path ---
# psycopg2-binary is required unconditionally: monitor/ingest_health.py, monitor/query_log.py,
# monitor/trend_monitor.py, analysis/supplement_summary.py, ingest/food.py, and lib/views.py
# all import psycopg2.extras at module level. The previous `; extra == "direct_role"` marker
# caused pip to silently skip this on plain `pip install -r requirements.txt` (including CI),
# leading to ImportError at module import time rather than a clean runtime error.
psycopg2-binary==2.9.11

# --- reports ---
openpyxl==3.1.5
reportlab==4.5.1

# --- dev / test ---
pytest==8.0.0
pytest-asyncio==0.23.0
